{
  "name": "ethglobal_bangkok2024",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w & sleep 1 && node --watch dist/index.js",
    "start": "node dist/index.js",
    "postinstall": "tsc",
    "format": "prettier '**/**/*.{ts}' --write",
    "format:check": "prettier '**/**/*.{ts}' --check"
  },
  "dependencies": {
    "@xmtp/message-kit": "1.1.8-beta.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "yarn@1.22.22",
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
